%table.table.table-hover.table-condensed
  %thead
    %tr
      %th
        = t('admin.shared.email')
      %th
        = t('.created_at')
      %th
        = t('.status')
  %tbody
    = render @subscriptions
